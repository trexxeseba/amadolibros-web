name: B11 editorial real — lote 2000

on:
  push:
    branches:
      - b11/editorial-real-2000-batch-01
    paths:
      - '.github/workflows/book-editorial-2000.yml'
      - 'scripts/seo/book-editorial-2000-plan.mjs'
      - 'functions/__tests__/book-editorial-2000-plan.test.js'
      - 'functions/_shared/book-editorial-upgrades.js'
      - 'functions/_shared/book-enrichment-registry.js'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/book-editorial-2000.yml'
      - 'scripts/seo/book-editorial-2000-plan.mjs'
      - 'functions/__tests__/book-editorial-2000-plan.test.js'
      - 'functions/_shared/book-editorial-upgrades.js'
      - 'functions/_shared/book-enrichment-registry.js'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: book-editorial-2000-${{ github.ref }}
  cancel-in-progress: true

jobs:
  plan-and-lock:
    name: Select 2000 ISBN and prove title immutability
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'

      - name: Validate code and title-lock tests
        run: |
          node --check scripts/seo/book-editorial-2000-plan.mjs
          node --test functions/__tests__/book-editorial-2000-plan.test.js

      - name: Build the real 2000-ISBN cohort
        env:
          BOOK_EDITORIAL_BATCH_LIMIT: '2000'
          BOOK_EDITORIAL_OUTPUT_DIR: artifacts/book-editorial/isbn-2000
        run: node scripts/seo/book-editorial-2000-plan.mjs

      - name: Enforce the 2000-title contract
        run: |
          node --input-type=module <<'NODE'
          import fs from 'node:fs';
          import {
            assertEditorialBatchPlan,
            titleFingerprint,
          } from './scripts/seo/book-editorial-2000-plan.mjs';

          const plan = JSON.parse(fs.readFileSync(
            'artifacts/book-editorial/isbn-2000/cohort.json',
            'utf8',
          ));
          assertEditorialBatchPlan(plan, 2000);
          if (plan.entries.some(entry =>
            entry.commercial_title_sha256 !== titleFingerprint(entry.commercial_title)
          )) {
            throw new Error('Una huella de título no coincide.');
          }
          if (plan.title_policy.commercial_title !== 'immutable_byte_for_byte') {
            throw new Error('La política de títulos no quedó bloqueada.');
          }
          console.log(JSON.stringify({
            selected: plan.selected_count,
            titles_changed: 0,
            policy: plan.title_policy,
          }, null, 2));
          NODE

      - name: Confirm the pilot also keeps the exact commercial title
        run: |
          node --input-type=module <<'NODE'
          import { BOOK_EDITORIAL_UPGRADES } from './functions/_shared/book-editorial-upgrades.js';
          const record = BOOK_EDITORIAL_UPGRADES.find(entry => entry.isbn === '9791388034435');
          const expected = 'Grandes Clasicos Tomo 11 Disney Pixar para Colorear';
          if (!record) throw new Error('Falta el piloto editorial Disney.');
          if (record.editorial.seo_title !== expected) {
            throw new Error('El título HTML del piloto no repite exactamente el título comercial.');
          }
          if (!record.editorial.paragraphs.join(' ').includes('100 ilustraciones misteriosas')) {
            throw new Error('El piloto perdió el contenido editorial real.');
          }
          console.log('Piloto: título exacto preservado y contenido editorial presente.');
          NODE

      - name: Verify the authorized pilot directly in Production
        run: |
          set -euo pipefail
          PRODUCT_URL='https://www.amadolibros.com/libro/MLU1453287196/grandes-clasicos-tomo-11-disney-pixar-para-colorear'
          curl --fail --location --silent --show-error --retry 3 \
            --output /tmp/product.html "$PRODUCT_URL"
          grep -Fq '<h1>Grandes Clasicos Tomo 11 Disney Pixar para Colorear</h1>' /tmp/product.html
          grep -Fq '100 ilustraciones misteriosas' /tmp/product.html
          grep -Fq 'coloreando números y zonas según un código de color' /tmp/product.html
          grep -Fq '¿Para quién está recomendado?' /tmp/product.html

          curl --fail --location --silent --show-error --retry 3 \
            --output /tmp/feed.xml 'https://www.amadolibros.com/feed.xml'
          node <<'NODE'
          const fs = require('node:fs');
          const xml = fs.readFileSync('/tmp/feed.xml', 'utf8');
          const marker = '<g:id>MLU1453287196</g:id>';
          const index = xml.indexOf(marker);
          if (index < 0) throw new Error('Merchant no contiene MLU1453287196.');
          const start = xml.lastIndexOf('<item>', index);
          const end = xml.indexOf('</item>', index);
          const block = xml.slice(start, end + 7);
          if (!block.includes('<g:title>Grandes Clasicos Tomo 11 Disney Pixar para Colorear</g:title>')) {
            throw new Error('Merchant alteró el título comercial.');
          }
          if (!block.includes('Libro para colorear por números Disney y Pixar')) {
            throw new Error('Merchant no recibió la descripción editorial enriquecida.');
          }
          if (!block.includes('100 ilustraciones misteriosas')) {
            throw new Error('Merchant perdió el contenido específico del piloto.');
          }
          console.log('Producción: ficha y Merchant verificados; título comercial intacto.');
          NODE

      - name: Publish summary
        run: cat artifacts/book-editorial/isbn-2000/summary.md >> "$GITHUB_STEP_SUMMARY"

      - name: Upload the 2000-ISBN cohort and title evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: editorial-real-2000-${{ github.run_id }}
          path: artifacts/book-editorial/isbn-2000/
          if-no-files-found: error
          retention-days: 30
