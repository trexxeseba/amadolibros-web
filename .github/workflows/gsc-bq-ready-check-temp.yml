name: GSC BigQuery readiness check temporary

on:
  push:
    branches:
      - agent/gsc-bq-ready-check
    paths:
      - '.github/workflows/gsc-bq-ready-check-temp.yml'

permissions:
  contents: read
  id-token: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Authenticate with Google
        uses: google-github-actions/auth@v3
        with:
          project_id: amado-libros-analytics
          workload_identity_provider: projects/667747565315/locations/global/workloadIdentityPools/github-actions/providers/amadolibros-web
          service_account: ga4-reporter@amado-libros-analytics.iam.gserviceaccount.com
          create_credentials_file: true
          export_environment_variables: true
      - uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: amado-libros-analytics
      - name: Check Search Console export tables
        shell: bash
        run: |
          set -euo pipefail
          PROJECT='amado-libros-analytics'
          DATASET='searchconsole'
          echo '=== DATASET ==='
          if ! bq --project_id="$PROJECT" show --format=prettyjson "$PROJECT:$DATASET"; then
            echo 'READY=false'
            exit 0
          fi
          echo '=== TABLES ==='
          bq --project_id="$PROJECT" ls --format=prettyjson "$PROJECT:$DATASET" || true
          ready=true
          for T in ExportLog searchdata_url_impression searchdata_site_impression; do
            echo "=== $T ==="
            if bq --project_id="$PROJECT" show --format=prettyjson "$PROJECT:$DATASET.$T" > "/tmp/$T.json" 2>"/tmp/$T.err"; then
              cat "/tmp/$T.json"
              rows=$(python - <<PY
import json
p=json.load(open('/tmp/$T.json'))
print(p.get('numRows','0'))
PY
)
              echo "TABLE=$T ROWS=$rows"
              if [ "${rows:-0}" = "0" ]; then ready=false; fi
            else
              cat "/tmp/$T.err" || true
              echo "TABLE=$T MISSING=true"
              ready=false
            fi
          done
          echo "READY=$ready"
