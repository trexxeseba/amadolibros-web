<section class="nfc-section">
  <div class="nfc-inner">
    <div class="nfc-card">
      <div class="nfc-text">
        <h2 class="nfc-heading">¿No encontrás lo que buscás?</h2>
        <p class="nfc-body">
          Consultanos por título, autor o ISBN. Si no está en stock, vemos si podemos conseguirlo por encargo.
        </p>
      </div>
      <button
        type="button"
        class="nfc-btn"
        onclick="document.dispatchEvent(new CustomEvent('amado:openSearch'))"
        aria-label="Consultar un libro por WhatsApp"
      >
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
          <circle cx="11" cy="11" r="7" stroke="currentColor" stroke-width="1.75"/>
          <path d="m17 17 3.5 3.5" stroke="currentColor" stroke-width="1.75" stroke-linecap="round"/>
        </svg>
        Consultar un libro
      </button>
    </div>
  </div>
</section>

<style>
  .nfc-section {
    background: var(--surface);
    border-top: 1px solid var(--border);
  }

  .nfc-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
  }

  @media (min-width: 768px) {
    .nfc-inner { padding: 4rem 2rem; }
  }

  .nfc-card {
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: var(--r-lg);
    padding: 2rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: flex-start;
  }

  @media (min-width: 640px) {
    .nfc-card {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      padding: 2rem 2.5rem;
      gap: 2rem;
    }
  }

  .nfc-text {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .nfc-heading {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: clamp(1.125rem, 2.5vw, 1.5rem);
    color: var(--ink);
    letter-spacing: -0.01em;
  }

  .nfc-body {
    font-size: 0.9375rem;
    color: var(--ink-2);
    line-height: 1.6;
    max-width: 52ch;
  }

  .nfc-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--ink);
    color: var(--surface);
    font-family: var(--font-ui);
    font-size: 0.9375rem;
    font-weight: 600;
    padding: 0.75rem 1.5rem;
    border-radius: 999px;
    border: none;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
    transition: opacity 0.15s;
  }
  .nfc-btn:hover { opacity: 0.82; }
</style>
