# SEO-BASELINE-0 — 2026-08-08

Estado: **en preparación; no habilita MERGE-76 hasta adjuntar los exports/capturas crudos de GSC y ejecutar los dos audits reproducibles.**

## Objetivo

Congelar el estado SEO previo al lote de limpieza de URLs legacy. Este ticket no modifica `functions/` ni `astro-front/`.

## Search Console — cobertura de indexación

Snapshot informado para 2026-08-08 (debe quedar respaldado por export/captura cruda en `docs/seo/baseline-2026-08-08/`):

| Categoría GSC | Cantidad |
|---|---:|
| Categoría 1 | 16.166 |
| Categoría 2 | 1.033 |
| Categoría 3 | 750 |
| Categoría 4 | 346 |
| Categoría 5 | 319 |
| Categoría 6 | 225 |
| Categoría 7 | 4 |

> Los nombres exactos de las siete categorías deben conservarse tal como aparecen en GSC en el CSV/captura original. No se renombran ni se infieren en este documento hasta tener el artefacto fuente.

## Search Analytics — 90 días

Período baseline: **2026-05-11 a 2026-08-08**, inclusive (90 días).

Se exportan dos reportes por API:

- dimensión `page`: clicks, impressions, ctr y position;
- dimensión `query`: clicks, impressions, ctr y position.

Workflow: `.github/workflows/gsc-export.yml`.

El workflow reutiliza `GA4_SERVICE_ACCOUNT_JSON` únicamente como credencial Google. Ese service account debe tener permiso de lectura sobre la propiedad de Search Console. La propiedad exacta se pasa por `workflow_dispatch` (`https://www.amadolibros.com/` o `sc-domain:amadolibros.com`, según la propiedad real configurada).

Salida esperada del workflow:

- `search-analytics-page.json`
- `search-analytics-page.csv`
- `search-analytics-query.json`
- `search-analytics-query.csv`
- `manifest.json`

Los JSON que integren el baseline deben copiarse sin transformación a `docs/seo/baseline-2026-08-08/`.

## Estadísticas de rastreo y sitemaps

Guardar en `docs/seo/baseline-2026-08-08/` los exports/capturas de GSC de:

- solicitudes de rastreo por día;
- desglose por respuesta;
- desglose por tipo de archivo;
- desglose por propósito;
- bytes descargados;
- estado de sitemaps: URLs enviadas e indexadas.

Cuando GSC no permita CSV, guardar captura completa y legible. No transcribir números manualmente como sustituto del artefacto fuente.

## Grafo de enlazado interno

Script: `scripts/seo/link-graph-audit.mjs`.

Fuentes:

- sitemap de producción;
- home;
- `/catalogo`;
- `/libros-maria-montessori-uruguay`;
- las ocho landings SEO autorizadas;
- cualquier paginación interna descubierta que matchee `/page/N` o `?page=N`.

Ejecución para congelar el baseline:

```bash
SEO_OUTPUT_DATE=2026-08-08 \
SEO_OUTPUT_DIR=docs/seo/baseline-2026-08-08 \
node scripts/seo/link-graph-audit.mjs
```

Checkpoint histórico esperado: aproximadamente **427 fichas linkeadas sobre 7.123 del sitemap (~6,0%)**, con tolerancia de ±1 punto porcentual por rotación de catálogo. El JSON ejecutado es la fuente de verdad; el checkpoint no se fuerza en código.

## Duplicación de catálogo

Script: `scripts/seo/duplication-report.mjs`.

Fuentes públicas, sin credenciales:

- `catalog.json` de R2;
- `catalog/manifest.json`;
- índice activo R2 actual;
- índice pausado R2 actual.

El reporte genera grupos activos por:

1. ISBN normalizado;
2. título + autor normalizados.

Cada grupo queda clasificado como `same_book` o `isbn_inconsistent` con la regla exacta documentada dentro del JSON. Los valores históricos de referencia mencionados en el análisis previo (3.201 / ~1.037 / ~2.164 / 194 en activas) se usan únicamente como control de plausibilidad hasta reproducirlos con el script; no se hardcodean como resultado.

Ejecución:

```bash
SEO_OUTPUT_DATE=2026-08-08 \
SEO_OUTPUT_DIR=docs/seo/baseline-2026-08-08 \
node scripts/seo/duplication-report.mjs
```

## Gate para MERGE-76

No mergear PR #76 hasta que se cumplan las cuatro condiciones:

1. exports/capturas crudos de cobertura, crawl stats y sitemaps guardados;
2. Search Analytics API guardado para page y query, 90 días;
3. `link-graph-audit-2026-08-08.json` generado y revisado;
4. `duplication-report-2026-08-08.json` generado y revisado.

Después de cumplir el gate: squash merge de PR #76, registrar timestamp exacto del deploy y ejecutar los smoke tests legacy definidos en MERGE-76.
