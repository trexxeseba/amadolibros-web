# SEO-BASELINE-0 — 2026-08-08

Estado: **baseline estructural capturado y audits públicos reproducidos.** Este ticket no modifica `functions/` ni `astro-front/`.

## Objetivo

Congelar el estado SEO previo al lote de limpieza de URLs legacy (PR #76), con artefactos versionados y scripts repetibles para comparar antes/después.

## Fuente de cobertura GSC ya preservada

El archivo de Search Console aportado el 2026-08-08 tiene **última actualización 2026-08-04**. Sus tablas fueron preservadas sin renombrar categorías en:

- `docs/seo/baseline-2026-08-08/coverage-reasons.csv`
- `docs/seo/baseline-2026-08-08/coverage-timeseries.csv`
- `docs/seo/baseline-2026-08-08/crawled-not-indexed-examples.csv`

### Estado de cobertura al último dato disponible (2026-08-04)

| Motivo GSC | Páginas |
|---|---:|
| Rastreada: actualmente sin indexar | 16.166 |
| Página alternativa con etiqueta canónica adecuada | 1.033 |
| No se ha encontrado (404) | 750 |
| Página con redirección | 346 |
| Duplicada: el usuario no ha indicado ninguna versión canónica | 319 |
| Excluida por una etiqueta `noindex` | 225 |
| Soft 404 | 4 |
| Bloqueada por robots.txt | 1 |
| Duplicada: Google ha elegido una versión canónica diferente a la del usuario | 3 |

Totales del gráfico de cobertura al 2026-08-04:

- **18.847** URLs sin indexar.
- **670** URLs indexadas.
- **19.517** URLs conocidas en ese snapshot.

Referencia histórica: el 2026-05-10 el mismo gráfico mostraba 186.012 sin indexar y 154 indexadas. Entre esos dos puntos las no indexadas bajaron aproximadamente 89,9% y las indexadas pasaron de 154 a 670. Esto es contexto descriptivo; no se atribuye causalidad a un cambio concreto sin medición adicional.

### Muestra preservada de `Rastreada: actualmente sin indexar`

La muestra manual aportada antes de PR #76 contiene 10 URLs: seis paginaciones/`add-to-cart`, tres `?book=MLU...` y una URL actual `/libro/...`. La muestra sirve para documentar patrones legacy, pero **no se usa para inferir qué porcentaje de las 16.166 pertenece a cada patrón**.

## Resultado reproducido: grafo interno

Workflow público ejecutado el 2026-08-08: `SEO baseline public audit`, run `31259055898`.

Resultado exacto:

- **7.123** fichas `/libro/` presentes en sitemap.
- **460** fichas con al menos un enlace HTML desde las páginas de descubrimiento auditadas.
- **6.663** fichas huérfanas bajo esta metodología.
- **6,46%** enlazadas / **93,54%** huérfanas.

Esto reemplaza el checkpoint preliminar de ~427/7.123. El valor reproducido es la fuente de verdad del baseline.

Script: `scripts/seo/link-graph-audit.mjs`.

Fuentes:

- sitemap de producción;
- home;
- `/catalogo`;
- `/libros-maria-montessori-uruguay`;
- las ocho landings SEO autorizadas;
- cualquier paginación interna descubierta que matchee `/page/N` o `?page=N`.

Ejecución:

```bash
SEO_OUTPUT_DATE=2026-08-08 \
SEO_OUTPUT_DIR=artifacts/seo \
node scripts/seo/link-graph-audit.mjs
```

## Resultado reproducido: duplicación

El mismo workflow produjo el reporte de duplicación con estas cifras:

Inventario:

- **7.123** items en `catalog.json`.
- **7.123** activos con stock usados para el análisis.
- **7.128** items en active-index.
- **10.038** items en paused-index.
- paridad active-index: 4 IDs sólo en catálogo y 9 sólo en índice.

Agrupación por ISBN normalizado:

- **3.164** grupos duplicados.
- **6.341** listings dentro de esos grupos.
- **211** grupos clasificados como `same_book` bajo la regla estricta del script.
- **2.953** grupos `isbn_inconsistent`.

Agrupación por título + autor normalizados:

- **225** grupos duplicados.
- **452** listings.
- **217** grupos `same_book`.
- **8** grupos con ISBN inconsistente.

Estos resultados reemplazan las estimaciones preliminares 3.201 / ~1.037 / ~2.164 / 194.

`same_book` significa **candidato a revisión**, no autorización automática para canonical/301. La decisión posterior debe considerar edición, editorial, idioma, formato, condición y diferencias comerciales cuando esos datos existan.

Script: `scripts/seo/duplication-report.mjs`.

Fuentes públicas:

- `catalog.json` de R2;
- `catalog/manifest.json`;
- índice activo R2 actual;
- índice pausado R2 actual.

Ejecución:

```bash
SEO_OUTPUT_DATE=2026-08-08 \
SEO_OUTPUT_DIR=artifacts/seo \
node scripts/seo/duplication-report.mjs
```

Resumen versionado: `docs/seo/baseline-2026-08-08/public-audit-summary.json`.

Los JSON completos fueron preservados como artifact del workflow `seo-baseline-public-2026-08-08`; el reporte puede regenerarse en cualquier momento con los scripts versionados.

## Search Analytics — 90 días

Período baseline fijado: **2026-05-11 a 2026-08-08**, inclusive.

Se exportan dos reportes por API:

- dimensión `page`: clicks, impressions, ctr y position;
- dimensión `query`: clicks, impressions, ctr y position.

Workflow: `.github/workflows/gsc-export.yml`.

Los workflows se autentican sin claves persistentes mediante Workload Identity Federation y la cuenta `ga4-reporter@amado-libros-analytics.iam.gserviceaccount.com`. Esa cuenta debe tener permiso de lectura sobre GA4 y sobre la propiedad de Search Console. La propiedad exacta de Search Console se pasa por `workflow_dispatch` (`https://www.amadolibros.com/` o `sc-domain:amadolibros.com`, según la propiedad real configurada).

Salida esperada:

- `search-analytics-page.json`
- `search-analytics-page.csv`
- `search-analytics-query.json`
- `search-analytics-query.csv`
- `manifest.json`

Como el período está fijado, esta extracción puede repetirse después sin perder el baseline histórico. Los JSON que integren el baseline deben copiarse sin transformación a `docs/seo/baseline-2026-08-08/`.

## Estadísticas de rastreo y sitemaps

Guardar los exports/capturas de GSC de:

- solicitudes de rastreo por día;
- desglose por respuesta;
- desglose por tipo de archivo;
- desglose por propósito;
- bytes descargados;
- estado de sitemaps: URLs enviadas e indexadas.

Cuando GSC no permita CSV, guardar captura completa y legible. La fecha/hora exacta del deploy de PR #76 será el punto de corte para separar antes/después en series históricas.

## Gate operativo para MERGE-76

Antes del merge deben quedar preservados como mínimo:

1. snapshot de cobertura y ejemplos legacy — **cumplido**;
2. scripts reproducibles de link graph y duplicación — **cumplido**;
3. ejecución reproducida de ambos audits — **cumplido**;
4. timestamp exacto del deploy de #76 — se registra al merge/deploy.

Search Analytics y crawl stats siguen siendo parte del baseline, pero al ser series históricas con rango fijo pueden extraerse después sin perder el estado pre-deploy. No deben confundirse con mediciones posteriores al cambio.

Después: squash merge de PR #76 y smoke tests en producción según MERGE-76.
