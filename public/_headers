# Cloudflare Pages — Cache headers
# HTML nunca cacheado → siempre sirve la versión más nueva del deploy
/*.html
  Cache-Control: no-store, must-revalidate

/
  Cache-Control: no-store, must-revalidate
